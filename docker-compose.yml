services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aaas
      POSTGRES_PASSWORD: aaas
      POSTGRES_DB: aaas
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
