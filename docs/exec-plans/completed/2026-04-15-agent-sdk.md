# Agent SDK 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `packages/agent-sdk` 创建 `@aaas/agent-sdk`，让开发者用一行 `createAgent()` 构建符合 AaaS harness 约定的 TypeScript agent，并用 TS SDK 重写 `packages/demo-agent`。

**Architecture:** SDK 通过 `registerApiProvider` 注册一个自定义 `gateway-llm` API 提供商，将 pi-coding-agent 的 LLM 调用路由到 `POST /gateway/llm`。运行时检测环境变量决定走 gateway 还是直连 provider。HarnessServer 暴露 `/health` 和 `/chat`，维护跨请求的内存 session。FileSync 在进程内运行，负责 `/persistent/` ↔ S3 的同步。pi SessionManager 指向 `/persistent/conversation/` 实现跨沙箱重启的 session 持久化。

**Spec:** `docs/product-specs/2026-04-15-agent-sdk-design.md`

**Tech Stack:** TypeScript, Node.js, `@mariozechner/pi-coding-agent`, `@mariozechner/pi-ai`, Express, vitest

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `packages/agent-sdk/package.json` | 新建 | 包定义，依赖 pi-coding-agent |
| `packages/agent-sdk/tsconfig.json` | 新建 | TS 配置 |
| `packages/agent-sdk/tsconfig.build.json` | 新建 | 构建专用 TS 配置 |
| `packages/agent-sdk/vitest.config.ts` | 新建 | 测试配置 |
| `packages/agent-sdk/src/environment.ts` | 新建 | 环境检测 + 配置解析（读 env vars） |
| `packages/agent-sdk/src/gateway-client.ts` | 新建 | Gateway HTTP 交互（appendMessages, presignUrls, listFiles） |
| `packages/agent-sdk/src/gateway-llm-adapter.ts` | 新建 | 实现 pi-ai ApiProvider 接口，转发 LLM 调用到 gateway |
| `packages/agent-sdk/src/file-sync.ts` | 新建 | TS 重写 file_sync.py：init（S3→本地）+ watch（本地→S3） |
| `packages/agent-sdk/src/harness-server.ts` | 新建 | HTTP server：GET /health, POST /chat，维护内存 session |
| `packages/agent-sdk/src/create-agent.ts` | 新建 | createAgent() 顶层 API，组装所有模块 |
| `packages/agent-sdk/src/index.ts` | 新建 | 公开 API 导出 |
| `packages/agent-sdk/src/cli/init.ts` | 新建 | `npx @aaas/agent-sdk init` 脚手架 |
| `packages/agent-sdk/src/cli/templates/` | 新建 | 项目模板文件 |
| `packages/agent-sdk/test/environment.test.ts` | 新建 | environment.ts 单元测试 |
| `packages/agent-sdk/test/gateway-client.test.ts` | 新建 | gateway-client.ts 单元测试 |
| `packages/agent-sdk/test/gateway-llm-adapter.test.ts` | 新建 | adapter 单元测试 |
| `packages/agent-sdk/test/file-sync.test.ts` | 新建 | file-sync.ts 单元测试 |
| `packages/agent-sdk/test/harness-server.test.ts` | 新建 | harness-server.ts 集成测试 |
| `packages/demo-agent/src/agent.ts` | 新建（替换） | 用 SDK 重写 demo-agent |
| `packages/demo-agent/e2b.Dockerfile` | 修改 | 从 Python 改为 Node.js 基础镜像 |
| `packages/demo-agent/package.json` | 新建 | Node.js 包定义 |
| `packages/dispatcher/src/sandbox.ts` | 修改 | 移除 file_sync + flask 启动步骤，改为 `node dist/agent.js` |
| `pnpm-workspace.yaml` | 修改 | 添加 agent-sdk 到 workspace |

---

## Task 1：初始化 `packages/agent-sdk` 包结构

**Files:**
- 新建: `packages/agent-sdk/package.json`
- 新建: `packages/agent-sdk/tsconfig.json`
- 新建: `packages/agent-sdk/tsconfig.build.json`
- 新建: `packages/agent-sdk/vitest.config.ts`
- 新建: `packages/agent-sdk/src/index.ts`（空占位）
- 修改: `pnpm-workspace.yaml`

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p packages/agent-sdk/src/cli/templates
mkdir -p packages/agent-sdk/test
```

- [ ] **Step 2: 创建 `packages/agent-sdk/package.json`**

```json
{
  "name": "@aaas/agent-sdk",
  "version": "0.1.0",
  "description": "AaaS Agent SDK — build agents that run on the AaaS harness",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "agent-sdk": "./dist/cli/init.js"
  },
  "files": ["dist"],
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "test": "vitest --run",
    "prepublishOnly": "pnpm clean && pnpm build"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^0.62.0",
    "@mariozechner/pi-ai": "^0.62.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^24.0.0",
    "shx": "^0.4.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "license": "MIT"
}
```

- [ ] **Step 3: 创建 `packages/agent-sdk/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src", "test"]
}
```

- [ ] **Step 4: 创建 `packages/agent-sdk/tsconfig.build.json`**

```json
{
  "extends": "./tsconfig.json",
  "exclude": ["test", "node_modules"]
}
```

- [ ] **Step 5: 创建 `packages/agent-sdk/vitest.config.ts`**

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
  },
})
```

- [ ] **Step 6: 创建空占位 `packages/agent-sdk/src/index.ts`**

```typescript
// exports added as modules are implemented
export {}
```

- [ ] **Step 7: 确认 `pnpm-workspace.yaml` 包含 agent-sdk**

检查文件内容：
```bash
cat pnpm-workspace.yaml
```

如果 `packages/*` 已存在则无需修改。如未覆盖，添加 `packages/agent-sdk`。

- [ ] **Step 8: 安装依赖**

```bash
pnpm install
```

- [ ] **Step 9: 验证包结构**

```bash
ls packages/agent-sdk/
# 期望: package.json tsconfig.json tsconfig.build.json vitest.config.ts src/ test/
pnpm --filter @aaas/agent-sdk build
# 期望: dist/ 生成，无报错
```

- [ ] **Step 10: Commit**

```bash
git add packages/agent-sdk/ pnpm-workspace.yaml pnpm-lock.yaml
git commit -m "feat(agent-sdk): initialize package structure"
```

---

## Task 2：`environment.ts` — 环境检测与配置解析

**Files:**
- 新建: `packages/agent-sdk/src/environment.ts`
- 新建: `packages/agent-sdk/test/environment.test.ts`

这是所有其他模块的基础。沙箱模式 vs 本地模式的所有行为差异都由此决定。

- [ ] **Step 1: 写失败测试**

`packages/agent-sdk/test/environment.test.ts`:

```typescript
import { afterEach, beforeEach, describe, expect, it } from 'vitest'
import { detectEnvironment, resolveConfig, type SandboxConfig } from '../src/environment.js'

describe('detectEnvironment', () => {
  const original = { ...process.env }

  afterEach(() => {
    // 恢复 env
    for (const key of ['GATEWAY_URL', 'SESSION_TOKEN', 'SESSION_ID']) {
      if (original[key] === undefined) delete process.env[key]
      else process.env[key] = original[key]
    }
  })

  it('returns "local" when env vars are absent', () => {
    delete process.env.GATEWAY_URL
    delete process.env.SESSION_TOKEN
    expect(detectEnvironment()).toBe('local')
  })

  it('returns "sandbox" when GATEWAY_URL and SESSION_TOKEN are set', () => {
    process.env.GATEWAY_URL = 'https://gw.example.com'
    process.env.SESSION_TOKEN = 'tok_abc'
    process.env.SESSION_ID = 'conv_123'
    expect(detectEnvironment()).toBe('sandbox')
  })

  it('returns "local" when only GATEWAY_URL is set (SESSION_TOKEN missing)', () => {
    process.env.GATEWAY_URL = 'https://gw.example.com'
    delete process.env.SESSION_TOKEN
    expect(detectEnvironment()).toBe('local')
  })
})

describe('resolveConfig', () => {
  afterEach(() => {
    for (const key of ['GATEWAY_URL', 'SESSION_TOKEN', 'SESSION_ID', 'PORT']) {
      delete process.env[key]
    }
  })

  it('returns sandbox config when env vars present', () => {
    process.env.GATEWAY_URL = 'https://gw.example.com'
    process.env.SESSION_TOKEN = 'tok_abc'
    process.env.SESSION_ID = 'conv_123'
    const config = resolveConfig()
    expect(config.mode).toBe('sandbox')
    if (config.mode === 'sandbox') {
      expect(config.gatewayUrl).toBe('https://gw.example.com')
      expect(config.sessionToken).toBe('tok_abc')
      expect(config.sessionId).toBe('conv_123')
      expect(config.port).toBe(8080)
      expect(config.persistentRoot).toBe('/persistent')
    }
  })

  it('returns local config when env vars absent', () => {
    const config = resolveConfig()
    expect(config.mode).toBe('local')
  })

  it('respects PORT env var', () => {
    process.env.GATEWAY_URL = 'https://gw.example.com'
    process.env.SESSION_TOKEN = 'tok'
    process.env.SESSION_ID = 'conv'
    process.env.PORT = '9090'
    const config = resolveConfig()
    expect(config.port).toBe(9090)
  })
})
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: FAIL — Cannot find module '../src/environment.js'
```

- [ ] **Step 3: 实现 `packages/agent-sdk/src/environment.ts`**

```typescript
export type Environment = 'sandbox' | 'local'

export interface SandboxConfig {
  mode: 'sandbox'
  gatewayUrl: string
  sessionToken: string
  sessionId: string
  port: number
  persistentRoot: string
}

export interface LocalConfig {
  mode: 'local'
  port: number
}

export type ResolvedConfig = SandboxConfig | LocalConfig

export function detectEnvironment(): Environment {
  const hasGateway = Boolean(process.env.GATEWAY_URL)
  const hasToken = Boolean(process.env.SESSION_TOKEN)
  return hasGateway && hasToken ? 'sandbox' : 'local'
}

export function resolveConfig(): ResolvedConfig {
  const port = parseInt(process.env.PORT ?? '8080', 10)

  if (detectEnvironment() === 'sandbox') {
    const gatewayUrl = process.env.GATEWAY_URL!.replace(/\/$/, '')
    const sessionToken = process.env.SESSION_TOKEN!
    const sessionId = process.env.SESSION_ID ?? ''
    const persistentRoot = process.env.PERSISTENT_ROOT ?? '/persistent'
    return { mode: 'sandbox', gatewayUrl, sessionToken, sessionId, port, persistentRoot }
  }

  return { mode: 'local', port }
}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: PASS — 所有 environment 测试通过
```

- [ ] **Step 5: 导出到 index.ts**

```typescript
export { detectEnvironment, resolveConfig } from './environment.js'
export type { Environment, LocalConfig, ResolvedConfig, SandboxConfig } from './environment.js'
```

- [ ] **Step 6: Commit**

```bash
git add packages/agent-sdk/src/environment.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/environment.test.ts
git commit -m "feat(agent-sdk): add environment detection"
```

---

## Task 3：`gateway-client.ts` — Gateway HTTP 客户端

**Files:**
- 新建: `packages/agent-sdk/src/gateway-client.ts`
- 新建: `packages/agent-sdk/test/gateway-client.test.ts`

封装所有与 gateway 的 HTTP 交互。gateway LLM 调用在 Task 4 单独处理。

- [ ] **Step 1: 写失败测试**

`packages/agent-sdk/test/gateway-client.test.ts`:

```typescript
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { GatewayClient } from '../src/gateway-client.js'

// mock global fetch
const fetchMock = vi.fn()
global.fetch = fetchMock

describe('GatewayClient', () => {
  let client: GatewayClient

  beforeEach(() => {
    client = new GatewayClient('https://gw.example.com', 'tok_abc')
    fetchMock.mockReset()
  })

  it('appendMessages sends correct payload', async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({ last_message_id: 'msg_2' }),
    })

    const result = await client.appendMessages('msg_1', [
      { role: 'assistant', content: [{ type: 'text', text: 'hello' }], source: 'sandbox' },
    ])

    expect(result.last_message_id).toBe('msg_2')
    expect(fetchMock).toHaveBeenCalledWith(
      'https://gw.example.com/gateway/messages/append',
      expect.objectContaining({
        method: 'POST',
        headers: expect.objectContaining({ Authorization: 'Bearer tok_abc' }),
      })
    )
  })

  it('appendMessages throws on non-ok response', async () => {
    fetchMock.mockResolvedValueOnce({
      ok: false,
      status: 409,
      json: async () => ({ error: { code: 'stale_write', message: 'stale', retryable: true, details: {} } }),
    })
    await expect(
      client.appendMessages('msg_old', [])
    ).rejects.toThrow('stale_write')
  })

  it('presignUrls returns url list', async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        urls: [{ path: 'shared/SOUL.md', url: 'https://s3/presigned', expires_in: 3600 }],
      }),
    })
    const result = await client.presignUrls([{ action: 'upload', path: 'shared/SOUL.md' }])
    expect(result).toHaveLength(1)
    expect(result[0].path).toBe('shared/SOUL.md')
  })

  it('listFiles returns file list', async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        files: [{ path: 'shared/SOUL.md', size: 100, last_modified: '2026-01-01T00:00:00Z' }],
      }),
    })
    const result = await client.listFiles('shared')
    expect(result).toHaveLength(1)
    expect(result[0].path).toBe('shared/SOUL.md')
  })
})
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: FAIL — Cannot find module '../src/gateway-client.js'
```

- [ ] **Step 3: 实现 `packages/agent-sdk/src/gateway-client.ts`**

```typescript
export interface GatewayMessage {
  role: string
  content: Array<{ type: string; text?: string; [key: string]: unknown }>
  source: 'sandbox'
}

export interface PresignOperation {
  action: 'upload' | 'download'
  path: string
}

export interface PresignedUrl {
  path: string
  url: string
  expires_in: number
}

export interface RemoteFile {
  path: string
  size: number
  last_modified: string
}

export class GatewayClient {
  constructor(
    private readonly gatewayUrl: string,
    private readonly token: string,
  ) {}

  private headers(): Record<string, string> {
    return {
      'Content-Type': 'application/json',
      Authorization: `Bearer ${this.token}`,
    }
  }

  private async request<T>(path: string, body: unknown): Promise<T> {
    const res = await fetch(`${this.gatewayUrl}${path}`, {
      method: 'POST',
      headers: this.headers(),
      body: JSON.stringify(body),
    })
    if (!res.ok) {
      const data = await res.json().catch(() => ({}))
      const code = (data as any)?.error?.code ?? `http_${res.status}`
      throw new Error(code)
    }
    return res.json() as Promise<T>
  }

  async appendMessages(
    expectedLastMessageId: string | null,
    messages: GatewayMessage[],
  ): Promise<{ last_message_id: string }> {
    return this.request('/gateway/messages/append', {
      expected_last_message_id: expectedLastMessageId,
      messages,
    })
  }

  async presignUrls(operations: PresignOperation[]): Promise<PresignedUrl[]> {
    const data = await this.request<{ urls: PresignedUrl[] }>('/gateway/storage/presign', { operations })
    return data.urls
  }

  async listFiles(prefix: 'shared' | 'conversation'): Promise<RemoteFile[]> {
    const data = await this.request<{ files: RemoteFile[] }>('/gateway/storage/list', { prefix })
    return data.files
  }
}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: PASS
```

- [ ] **Step 5: 导出到 index.ts**

在 `packages/agent-sdk/src/index.ts` 追加：

```typescript
export { GatewayClient } from './gateway-client.js'
export type { GatewayMessage, PresignOperation, PresignedUrl, RemoteFile } from './gateway-client.js'
```

- [ ] **Step 6: Commit**

```bash
git add packages/agent-sdk/src/gateway-client.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/gateway-client.test.ts
git commit -m "feat(agent-sdk): add GatewayClient"
```

---

## Task 4：`gateway-llm-adapter.ts` — LLM 路由适配器

**Files:**
- 新建: `packages/agent-sdk/src/gateway-llm-adapter.ts`
- 新建: `packages/agent-sdk/test/gateway-llm-adapter.test.ts`

实现 pi-ai 的 `ApiProvider` 接口，将 LLM 调用转发到 gateway。因为 gateway `/llm` 是非流式的，适配器需把完整响应包装成一次性 event stream 发出。

注意：gateway 目前只支持 `glm-5.1` 模型（`ALLOWED_MODELS`）。适配器注册一个自定义 `api: 'gateway-llm'`，model 配置中 `baseUrl` 指向 gateway，model `id` 传给 gateway 的 `model` 字段。

- [ ] **Step 1: 查看 pi-ai 的 AssistantMessageEventStream 格式**

```bash
grep -n "AssistantMessageEventStream\|emit\|start\|delta\|stop" \
  /Users/fanfei/monorepo/zai-adk-ts/packages/ai/src/utils/event-stream.ts 2>/dev/null | head -30
```

理解 event stream 的协议：需要 emit `start`，然后 `delta`（文本内容），最后 `stop`。

- [ ] **Step 2: 写失败测试**

`packages/agent-sdk/test/gateway-llm-adapter.test.ts`:

```typescript
import { describe, expect, it, vi } from 'vitest'
import { createGatewayLlmProvider } from '../src/gateway-llm-adapter.js'

const fetchMock = vi.fn()
global.fetch = fetchMock

describe('createGatewayLlmProvider', () => {
  it('returns an ApiProvider with api = gateway-llm', () => {
    const provider = createGatewayLlmProvider('https://gw.example.com', 'tok')
    expect(provider.api).toBe('gateway-llm')
    expect(typeof provider.stream).toBe('function')
    expect(typeof provider.streamSimple).toBe('function')
  })

  it('streamSimple calls POST /gateway/llm and emits text content', async () => {
    fetchMock.mockResolvedValueOnce({
      ok: true,
      json: async () => ({
        message: {
          role: 'assistant',
          content: [{ type: 'text', text: 'hello world' }],
          tool_calls: [],
        },
        usage: { input_tokens: 10, output_tokens: 5, total_tokens: 15 },
      }),
    })

    const provider = createGatewayLlmProvider('https://gw.example.com', 'tok')
    const fakeModel = { id: 'glm-5.1', api: 'gateway-llm' } as any
    const fakeContext = {
      messages: [{ role: 'user', content: [{ type: 'text', text: 'hi' }] }],
    } as any

    const stream = provider.streamSimple(fakeModel, fakeContext, {})
    const events: any[] = []
    for await (const event of stream) {
      events.push(event)
    }

    expect(fetchMock).toHaveBeenCalledWith(
      'https://gw.example.com/gateway/llm',
      expect.objectContaining({ method: 'POST' })
    )
    // stream should emit a done event
    expect(events.some((e) => e.type === 'done')).toBe(true)
  })
})
```

- [ ] **Step 3: 运行测试，确认失败**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: FAIL — Cannot find module '../src/gateway-llm-adapter.js'
```

- [ ] **Step 4: 实现 `packages/agent-sdk/src/gateway-llm-adapter.ts`**

`AssistantMessageEvent` 的正确格式（来自 `@mariozechner/pi-ai` types.ts）：
- 必须以 `{ type: 'start', partial: AssistantMessage }` 开头
- 文本内容用 `{ type: 'text_delta', contentIndex: number, delta: string, partial: AssistantMessage }`
- 成功结束用 `{ type: 'done', reason: 'stop', message: AssistantMessage }`
- 错误结束用 `{ type: 'error', reason: 'error', error: AssistantMessage }`

`AssistantMessage` 结构：`{ role: 'assistant', content: TextContent[], api, provider, model, usage: { input, output, cacheRead, cacheWrite, totalTokens, cost: {...} }, stopReason, timestamp }`

```typescript
import type {
  ApiProvider,
  AssistantMessage,
  AssistantMessageEventStream,
  Context,
  SimpleStreamOptions,
} from '@mariozechner/pi-ai'

const GATEWAY_PROVIDER = 'gateway' as const
const GATEWAY_LLM_API = 'gateway-llm' as const

function makeAssistantMessage(
  text: string,
  modelId: string,
  inputTokens: number,
  outputTokens: number,
  stopReason: 'stop' | 'error' = 'stop',
  errorMessage?: string,
): AssistantMessage {
  const zeroUsage = { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, totalTokens: 0, cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0, total: 0 } }
  return {
    role: 'assistant',
    content: text ? [{ type: 'text', text }] : [],
    api: GATEWAY_LLM_API,
    provider: GATEWAY_PROVIDER,
    model: modelId,
    usage: { ...zeroUsage, input: inputTokens, output: outputTokens, totalTokens: inputTokens + outputTokens },
    stopReason,
    errorMessage,
    timestamp: Date.now(),
  } as unknown as AssistantMessage
}

export function createGatewayLlmProvider(
  gatewayUrl: string,
  token: string,
): ApiProvider<typeof GATEWAY_LLM_API, SimpleStreamOptions> {
  const baseUrl = gatewayUrl.replace(/\/$/, '')

  function streamSimple(
    model: { id: string; api: string },
    context: Context,
    _options?: SimpleStreamOptions,
  ): AssistantMessageEventStream {
    return (async function* () {
      const emptyPartial = makeAssistantMessage('', model.id, 0, 0)

      // 必须先 emit start
      yield { type: 'start' as const, partial: emptyPartial }

      const messages = context.messages.map((m: any) => ({ role: m.role, content: m.content }))

      const res = await fetch(`${baseUrl}/gateway/llm`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` },
        body: JSON.stringify({ model: model.id, messages }),
      })

      if (!res.ok) {
        const data = await res.json().catch(() => ({}))
        const errMsg = (data as any)?.error?.message ?? `http_${res.status}`
        const errMessage = makeAssistantMessage('', model.id, 0, 0, 'error', errMsg)
        yield { type: 'error' as const, reason: 'error' as const, error: errMessage }
        return
      }

      const data = await res.json() as {
        message: { role: string; content: any[]; tool_calls: any[] }
        usage: { input_tokens: number; output_tokens: number; total_tokens: number }
      }

      const textBlock = data.message.content.find((b: any) => b.type === 'text')
      const text = textBlock?.text ?? ''
      const { input_tokens, output_tokens } = data.usage

      // emit text delta
      const partial = makeAssistantMessage(text, model.id, input_tokens, output_tokens)
      yield { type: 'text_start' as const, contentIndex: 0, partial }
      yield { type: 'text_delta' as const, contentIndex: 0, delta: text, partial }
      yield { type: 'text_end' as const, contentIndex: 0, content: text, partial }

      // emit done
      yield { type: 'done' as const, reason: 'stop' as const, message: partial }
    })() as AssistantMessageEventStream
  }

  return {
    api: GATEWAY_LLM_API,
    stream: streamSimple as any,
    streamSimple,
  }
}
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: PASS
```

- [ ] **Step 6: 导出到 index.ts**

```typescript
export { createGatewayLlmProvider } from './gateway-llm-adapter.js'
```

- [ ] **Step 7: Commit**

```bash
git add packages/agent-sdk/src/gateway-llm-adapter.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/gateway-llm-adapter.test.ts
git commit -m "feat(agent-sdk): add gateway LLM adapter"
```

---

## Task 5：`file-sync.ts` — 进程内文件同步

**Files:**
- 新建: `packages/agent-sdk/src/file-sync.ts`
- 新建: `packages/agent-sdk/test/file-sync.test.ts`

TypeScript 重写 `file_sync.py` 的逻辑，在进程内运行（无独立进程）。

- [ ] **Step 1: 写失败测试**

`packages/agent-sdk/test/file-sync.test.ts`:

```typescript
import { mkdirSync, mkdtempSync, writeFileSync } from 'fs'
import { tmpdir } from 'os'
import { join } from 'path'
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { FileSync } from '../src/file-sync.js'
import type { GatewayClient } from '../src/gateway-client.js'

describe('FileSync', () => {
  let tmpDir: string
  let mockGateway: GatewayClient

  beforeEach(() => {
    tmpDir = mkdtempSync(join(tmpdir(), 'file-sync-test-'))
    mkdirSync(join(tmpDir, 'shared'), { recursive: true })
    mkdirSync(join(tmpDir, 'conversation'), { recursive: true })

    mockGateway = {
      listFiles: vi.fn(),
      presignUrls: vi.fn(),
      appendMessages: vi.fn(),
    } as any
  })

  it('init: downloads files listed by gateway', async () => {
    vi.mocked(mockGateway.listFiles).mockImplementation(async (prefix) => {
      if (prefix === 'shared') return [{ path: 'shared/SOUL.md', size: 10, last_modified: '' }]
      return []
    })
    vi.mocked(mockGateway.presignUrls).mockResolvedValue([
      { path: 'shared/SOUL.md', url: 'https://s3/presigned', expires_in: 3600 },
    ])

    // mock the actual download fetch
    const fetchMock = vi.fn().mockResolvedValue({
      ok: true,
      arrayBuffer: async () => Buffer.from('# SOUL').buffer,
    })
    global.fetch = fetchMock

    const sync = new FileSync(mockGateway, tmpDir)
    await sync.init()

    expect(mockGateway.listFiles).toHaveBeenCalledWith('shared')
    expect(mockGateway.listFiles).toHaveBeenCalledWith('conversation')
    expect(mockGateway.presignUrls).toHaveBeenCalled()
    expect(fetchMock).toHaveBeenCalledWith('https://s3/presigned')
  })

  it('watch: detects new files and uploads', async () => {
    vi.mocked(mockGateway.presignUrls).mockResolvedValue([
      { path: 'shared/new.md', url: 'https://s3/upload', expires_in: 3600 },
    ])
    const fetchMock = vi.fn().mockResolvedValue({ ok: true })
    global.fetch = fetchMock

    const sync = new FileSync(mockGateway, tmpDir, 50) // 50ms poll interval for test

    // start watch in background
    sync.startWatch()

    // create a new file
    await new Promise((r) => setTimeout(r, 10))
    writeFileSync(join(tmpDir, 'shared', 'new.md'), '# new')

    // wait for at least one poll cycle
    await new Promise((r) => setTimeout(r, 150))
    sync.stopWatch()

    // upload should have been called
    expect(fetchMock).toHaveBeenCalledWith('https://s3/upload', expect.objectContaining({ method: 'PUT' }))
  })
})
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: FAIL — Cannot find module '../src/file-sync.js'
```

- [ ] **Step 3: 实现 `packages/agent-sdk/src/file-sync.ts`**

```typescript
import { existsSync, mkdirSync, readdirSync, readFileSync, statSync, writeFileSync } from 'fs'
import { join, relative } from 'path'
import type { GatewayClient } from './gateway-client.js'

const POLL_INTERVAL_MS = 10_000

export class FileSync {
  private baseline = new Map<string, number>() // relative path → mtime
  private timer: ReturnType<typeof setInterval> | null = null

  constructor(
    private readonly gateway: GatewayClient,
    private readonly root: string,
    private readonly pollIntervalMs: number = POLL_INTERVAL_MS,
  ) {}

  /** Download all existing files from S3 to local /persistent/. */
  async init(): Promise<void> {
    mkdirSync(join(this.root, 'shared'), { recursive: true })
    mkdirSync(join(this.root, 'conversation'), { recursive: true })

    for (const prefix of ['shared', 'conversation'] as const) {
      const files = await this.gateway.listFiles(prefix)
      if (files.length === 0) continue

      const ops = files.map((f) => ({ action: 'download' as const, path: f.path }))
      const urls = await this.gateway.presignUrls(ops)

      for (const { path, url } of urls) {
        const localPath = join(this.root, path)
        mkdirSync(join(localPath, '..'), { recursive: true })
        const res = await fetch(url)
        if (!res.ok) {
          console.warn(`[file-sync] Failed to download ${path}: ${res.status}`)
          continue
        }
        const buf = await res.arrayBuffer()
        writeFileSync(localPath, Buffer.from(buf))
      }
    }

    this.baseline = this.scanFiles()
  }

  /** Start background polling loop — uploads changed files to S3 every pollIntervalMs. */
  startWatch(): void {
    this.baseline = this.scanFiles()
    this.timer = setInterval(() => {
      this.syncCycle().catch((err) => console.warn('[file-sync] sync cycle failed:', err))
    }, this.pollIntervalMs)
  }

  stopWatch(): void {
    if (this.timer) {
      clearInterval(this.timer)
      this.timer = null
    }
  }

  private scanFiles(): Map<string, number> {
    const result = new Map<string, number>()
    this.walkDir(this.root, result)
    return result
  }

  private walkDir(dir: string, result: Map<string, number>): void {
    if (!existsSync(dir)) return
    for (const entry of readdirSync(dir, { withFileTypes: true })) {
      const full = join(dir, entry.name)
      if (entry.isDirectory()) {
        this.walkDir(full, result)
      } else if (entry.isFile()) {
        const rel = relative(this.root, full)
        result.set(rel, statSync(full).mtimeMs)
      }
    }
  }

  private async syncCycle(): Promise<void> {
    const current = this.scanFiles()
    const changed: string[] = []

    for (const [path, mtime] of current) {
      const prev = this.baseline.get(path)
      if (prev === undefined || mtime > prev) changed.push(path)
    }

    if (changed.length === 0) return

    // Upload in batches of 100
    for (let i = 0; i < changed.length; i += 100) {
      const batch = changed.slice(i, i + 100)
      const ops = batch.map((p) => ({ action: 'upload' as const, path: p }))
      const urls = await this.gateway.presignUrls(ops)

      for (const { path, url } of urls) {
        const localPath = join(this.root, path)
        if (!existsSync(localPath)) continue
        try {
          const data = readFileSync(localPath)
          const res = await fetch(url, { method: 'PUT', body: data })
          if (!res.ok) console.warn(`[file-sync] Upload failed for ${path}: ${res.status}`)
        } catch (err) {
          console.warn(`[file-sync] Upload error for ${path}:`, err)
        }
      }
    }

    this.baseline = this.scanFiles()
  }
}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: PASS
```

- [ ] **Step 5: 导出到 index.ts**

```typescript
export { FileSync } from './file-sync.js'
```

- [ ] **Step 6: Commit**

```bash
git add packages/agent-sdk/src/file-sync.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/file-sync.test.ts
git commit -m "feat(agent-sdk): add FileSync"
```

---

## Task 6：`harness-server.ts` — HTTP 服务器与 agent loop

**Files:**
- 新建: `packages/agent-sdk/src/harness-server.ts`
- 新建: `packages/agent-sdk/test/harness-server.test.ts`

这是核心模块。维护内存中的 pi agent session，暴露 `/health` 和 `/chat`，在沙箱模式下会在 chat 后 fire-and-forget appendMessages。

- [ ] **Step 1: 写失败测试**

`packages/agent-sdk/test/harness-server.test.ts`:

```typescript
import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest'
import request from 'supertest'  // 需添加 supertest devDependency
import { createHarnessApp } from '../src/harness-server.js'

// mock createAgentSession
vi.mock('@mariozechner/pi-coding-agent', () => ({
  createAgentSession: vi.fn().mockResolvedValue({
    session: {
      prompt: vi.fn().mockResolvedValue(undefined),
      subscribe: vi.fn().mockReturnValue(() => {}),
      systemPrompt: 'You are a helpful assistant.',
      agent: { setSystemPrompt: vi.fn() },
    },
    modelFallbackMessage: undefined,
  }),
  SessionManager: {
    continueRecent: vi.fn().mockReturnValue({}),
    create: vi.fn().mockReturnValue({}),
    inMemory: vi.fn().mockReturnValue({}),
  },
}))

describe('HarnessServer', () => {
  it('GET /health returns { ok: true }', async () => {
    const app = await createHarnessApp({ systemPrompt: 'test', mode: 'local' } as any)
    const res = await request(app).get('/health')
    expect(res.status).toBe(200)
    expect(res.body).toEqual({ ok: true })
  })

  it('POST /chat with missing message returns 400', async () => {
    const app = await createHarnessApp({ systemPrompt: 'test', mode: 'local' } as any)
    const res = await request(app).post('/chat').send({})
    expect(res.status).toBe(400)
  })

  it('POST /chat returns reply', async () => {
    const { createAgentSession } = await import('@mariozechner/pi-coding-agent')
    const mockSession = {
      prompt: vi.fn().mockResolvedValue(undefined),
      subscribe: vi.fn((listener: any) => {
        // simulate agent_end event
        setTimeout(() => listener({ type: 'agent_end' }), 0)
        // simulate a message_update event with reply text
        setTimeout(() => listener({
          type: 'message_update',
          message: { role: 'assistant', content: [{ type: 'text', text: 'Hello!' }] }
        }), 0)
        return () => {}
      }),
      systemPrompt: '',
      agent: { setSystemPrompt: vi.fn() },
    }
    vi.mocked(createAgentSession).mockResolvedValue({
      session: mockSession,
      modelFallbackMessage: undefined,
      extensionsResult: {} as any,
    })

    const app = await createHarnessApp({ systemPrompt: 'test', mode: 'local' } as any)
    const res = await request(app).post('/chat').send({ message: 'hi' })
    expect(res.status).toBe(200)
    expect(res.body).toHaveProperty('reply')
  })
})
```

> **注意：** 需要添加 `supertest` 到 devDependencies：`pnpm --filter @aaas/agent-sdk add -D supertest @types/supertest`

- [ ] **Step 2: 运行测试，确认失败**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: FAIL — Cannot find module '../src/harness-server.js'
```

- [ ] **Step 3: 实现 `packages/agent-sdk/src/harness-server.ts`**

```typescript
import {
  createAgentSession,
  type CreateAgentSessionOptions,
  SessionManager,
  type ToolDefinition,
} from '@mariozechner/pi-coding-agent'
import { registerApiProvider } from '@mariozechner/pi-ai'
import express, { type Application } from 'express'
import { join } from 'path'
import { createGatewayLlmProvider } from './gateway-llm-adapter.js'
import type { GatewayClient } from './gateway-client.js'
import type { ResolvedConfig } from './environment.js'

export interface HarnessServerOptions {
  systemPrompt: string
  tools?: ToolDefinition[]
  skillDirs?: string[]
  config: ResolvedConfig
  gateway?: GatewayClient
}

export async function createHarnessApp(options: HarnessServerOptions): Promise<Application> {
  const { config, systemPrompt, tools = [], gateway } = options
  const app = express()
  app.use(express.json())

  // Register gateway LLM provider in sandbox mode
  if (config.mode === 'sandbox' && gateway) {
    const provider = createGatewayLlmProvider(config.gatewayUrl, config.sessionToken)
    registerApiProvider(provider, 'aaas-gateway')
  }

  // Resolve session directory
  const sessionDir = config.mode === 'sandbox'
    ? join(config.persistentRoot, 'conversation')
    : undefined // pi defaults to ~/.pi/agent/sessions in local mode

  // Create pi session (reused across requests within this process)
  const sessionManager = sessionDir
    ? SessionManager.continueRecent(process.cwd(), sessionDir)
    : SessionManager.inMemory()

  // In sandbox mode, construct a Model object pointing to gateway-llm provider and pass it directly
  // to createAgentSession — this bypasses ModelRegistry.findInitialModel() which requires an API key
  let gatewayModel: import('@mariozechner/pi-ai').Model<any> | undefined
  if (config.mode === 'sandbox') {
    gatewayModel = {
      id: process.env.LLM_MODEL ?? 'glm-5.1',
      name: 'Gateway LLM',
      api: 'gateway-llm',
      provider: 'gateway' as any,
      baseUrl: config.gatewayUrl,
      reasoning: false,
      input: ['text' as const],
      cost: { input: 0, output: 0, cacheRead: 0, cacheWrite: 0 },
      contextWindow: 128000,
      maxTokens: 4096,
    } as import('@mariozechner/pi-ai').Model<any>
  }

  const sessionOptions: CreateAgentSessionOptions = {
    sessionManager,
    customTools: tools,
    ...(gatewayModel && { model: gatewayModel }),
  }

  const { session } = await createAgentSession(sessionOptions)

  if (systemPrompt) {
    session.agent.setSystemPrompt(systemPrompt)
  }

  // Track last assistant reply across events
  let lastReply = ''

  app.get('/health', (_req, res) => {
    res.json({ ok: true })
  })

  app.post('/chat', async (req, res) => {
    const { message } = req.body as { message?: string }
    if (!message) {
      res.status(400).json({ error: 'missing message' })
      return
    }

    lastReply = ''

    await new Promise<void>((resolve) => {
      const unsubscribe = session.subscribe((event) => {
        if (event.type === 'message_update') {
          const content = event.message.content
          if (Array.isArray(content)) {
            for (const block of content) {
              if (block.type === 'text' && event.message.role === 'assistant') {
                lastReply = block.text ?? ''
              }
            }
          }
        }
        if (event.type === 'agent_end') {
          unsubscribe()
          resolve()
        }
      })
      session.prompt(message)
    })

    // Fire-and-forget: audit to gateway in sandbox mode
    if (config.mode === 'sandbox' && gateway && lastReply) {
      gateway.appendMessages(null, [{
        role: 'assistant',
        content: [{ type: 'text', text: lastReply }],
        source: 'sandbox',
      }]).catch((err) => console.warn('[harness] appendMessages failed:', err))
    }

    res.json({ reply: lastReply })
  })

  return app
}
```

- [ ] **Step 4: 安装 supertest**

```bash
pnpm --filter @aaas/agent-sdk add -D supertest @types/supertest
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
pnpm --filter @aaas/agent-sdk test
# 期望: PASS（或根据 pi mock 行为调整 reply 提取逻辑）
```

- [ ] **Step 6: 导出到 index.ts**

```typescript
export { createHarnessApp } from './harness-server.js'
export type { HarnessServerOptions } from './harness-server.js'
```

- [ ] **Step 7: Commit**

```bash
git add packages/agent-sdk/src/harness-server.ts packages/agent-sdk/src/index.ts packages/agent-sdk/test/harness-server.test.ts package.json pnpm-lock.yaml
git commit -m "feat(agent-sdk): add HarnessServer"
```

---

## Task 7：`create-agent.ts` — 顶层 API

**Files:**
- 新建: `packages/agent-sdk/src/create-agent.ts`

组装所有模块，提供开发者最终使用的一行 API。

- [ ] **Step 1: 实现 `packages/agent-sdk/src/create-agent.ts`**

无需单独测试（是组装层，各模块已有测试）。

```typescript
import { createServer } from 'http'
import type { ToolDefinition } from '@mariozechner/pi-coding-agent'
import { resolveConfig } from './environment.js'
import { FileSync } from './file-sync.js'
import { GatewayClient } from './gateway-client.js'
import { createHarnessApp } from './harness-server.js'

export interface CreateAgentOptions {
  /** System prompt for the agent */
  systemPrompt?: string
  /** Custom tools to register */
  tools?: ToolDefinition[]
  /** Directories to load skills from */
  skillDirs?: string[]
}

export async function createAgent(options: CreateAgentOptions = {}): Promise<void> {
  const { systemPrompt = 'You are a helpful assistant.', tools, skillDirs } = options
  const config = resolveConfig()

  let gateway: GatewayClient | undefined
  let fileSync: FileSync | undefined

  if (config.mode === 'sandbox') {
    gateway = new GatewayClient(config.gatewayUrl, config.sessionToken)
    fileSync = new FileSync(gateway, config.persistentRoot)

    console.log('[agent-sdk] Sandbox mode: initializing file sync...')
    await fileSync.init()
    fileSync.startWatch()
    console.log('[agent-sdk] File sync started.')
  } else {
    console.log('[agent-sdk] Local mode: skipping file sync.')
  }

  const app = await createHarnessApp({ systemPrompt, tools, skillDirs, config, gateway })

  const server = createServer(app)
  server.listen(config.port, () => {
    console.log(`[agent-sdk] Agent listening on port ${config.port} (${config.mode} mode)`)
  })

  // Graceful shutdown
  process.on('SIGTERM', () => {
    fileSync?.stopWatch()
    server.close(() => process.exit(0))
  })
}
```

- [ ] **Step 2: 导出到 index.ts**

```typescript
export { createAgent } from './create-agent.js'
export type { CreateAgentOptions } from './create-agent.js'
```

- [ ] **Step 3: 构建确认**

```bash
pnpm --filter @aaas/agent-sdk build
# 期望: 无报错，dist/ 生成
pnpm --filter @aaas/agent-sdk test
# 期望: 全部通过
```

- [ ] **Step 4: Commit**

```bash
git add packages/agent-sdk/src/create-agent.ts packages/agent-sdk/src/index.ts
git commit -m "feat(agent-sdk): add createAgent() top-level API"
```

---

## Task 8：CLI 脚手架 `npx @aaas/agent-sdk init`

**Files:**
- 新建: `packages/agent-sdk/src/cli/templates/agent.ts.template`
- 新建: `packages/agent-sdk/src/cli/templates/e2b.Dockerfile.template`
- 新建: `packages/agent-sdk/src/cli/templates/e2b.toml.template`
- 新建: `packages/agent-sdk/src/cli/templates/package.json.template`
- 新建: `packages/agent-sdk/src/cli/templates/tsconfig.json.template`
- 新建: `packages/agent-sdk/src/cli/templates/README.md.template`
- 新建: `packages/agent-sdk/src/cli/init.ts`

- [ ] **Step 1: 创建模板文件**

`src/cli/templates/agent.ts.template`:
```typescript
import { createAgent } from '@aaas/agent-sdk'

createAgent({
  systemPrompt: 'You are a helpful assistant.',
  // tools: [],
  // skillDirs: ['./skills/'],
})
```

`src/cli/templates/e2b.Dockerfile.template`:
```dockerfile
FROM node:20-slim
WORKDIR /app
COPY package.json .
RUN npm install --production
COPY dist/ ./dist/
RUN mkdir -p /persistent/shared /persistent/conversation
CMD ["node", "dist/agent.js"]
```

`src/cli/templates/e2b.toml.template`:
```toml
dockerfile = "e2b.Dockerfile"
# After running `e2b template build`, fill in template_id:
# template_id = ""
```

`src/cli/templates/package.json.template` (使用 `{{name}}` 占位符):
```json
{
  "name": "{{name}}",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/agent.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/agent.ts",
    "start": "node dist/agent.js"
  },
  "dependencies": {
    "@aaas/agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
```

`src/cli/templates/tsconfig.json.template`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src"]
}
```

`src/cli/templates/README.md.template`:
```markdown
# {{name}}

An AI agent built with `@aaas/agent-sdk`.

## Development

\`\`\`bash
# Install dependencies
npm install

# Run locally (direct LLM, requires API key)
ANTHROPIC_API_KEY=sk-ant-... npm run dev

# Test the agent
curl -X POST http://localhost:8080/chat -H 'Content-Type: application/json' -d '{"message":"hello"}'
\`\`\`

## Deploy to AaaS

\`\`\`bash
# Build
npm run build

# Build e2b template
e2b template build

# Register template ID in e2b.toml, then register with AaaS platform
\`\`\`
```

- [ ] **Step 2: 实现 `src/cli/init.ts`**

```typescript
#!/usr/bin/env node
import { existsSync, mkdirSync, readFileSync, writeFileSync } from 'fs'
import { join, resolve } from 'path'
import { fileURLToPath } from 'url'

const __dirname = fileURLToPath(new URL('.', import.meta.url))
const TEMPLATES_DIR = join(__dirname, 'templates')

function renderTemplate(content: string, vars: Record<string, string>): string {
  return content.replace(/\{\{(\w+)\}\}/g, (_, key) => vars[key] ?? `{{${key}}}`)
}

function main() {
  const name = process.argv[2]
  if (!name) {
    console.error('Usage: npx @aaas/agent-sdk init <project-name>')
    process.exit(1)
  }

  const targetDir = resolve(process.cwd(), name)
  if (existsSync(targetDir)) {
    console.error(`Directory "${name}" already exists.`)
    process.exit(1)
  }

  mkdirSync(join(targetDir, 'src', 'skills'), { recursive: true })

  const vars = { name }
  const files: Array<[string, string]> = [
    ['agent.ts.template', 'src/agent.ts'],
    ['e2b.Dockerfile.template', 'e2b.Dockerfile'],
    ['e2b.toml.template', 'e2b.toml'],
    ['package.json.template', 'package.json'],
    ['tsconfig.json.template', 'tsconfig.json'],
    ['README.md.template', 'README.md'],
  ]

  for (const [template, output] of files) {
    const content = readFileSync(join(TEMPLATES_DIR, template), 'utf-8')
    writeFileSync(join(targetDir, output), renderTemplate(content, vars))
  }

  console.log(`✓ Created ${name}/`)
  console.log(`\nNext steps:`)
  console.log(`  cd ${name}`)
  console.log(`  npm install`)
  console.log(`  ANTHROPIC_API_KEY=sk-ant-... npm run dev`)
}

main()
```

- [ ] **Step 3: 构建并手动验证**

```bash
pnpm --filter @aaas/agent-sdk build
node packages/agent-sdk/dist/cli/init.js test-agent
ls test-agent/
# 期望: src/ e2b.Dockerfile e2b.toml package.json tsconfig.json README.md
cat test-agent/src/agent.ts
# 期望: createAgent() 代码
rm -rf test-agent
```

- [ ] **Step 4: Commit**

```bash
git add packages/agent-sdk/src/cli/
git commit -m "feat(agent-sdk): add npx init CLI scaffolding"
```

---

## Task 9：用 TS SDK 重写 `packages/demo-agent`

**Files:**
- 新建: `packages/demo-agent/src/agent.ts`
- 新建: `packages/demo-agent/package.json`
- 新建: `packages/demo-agent/tsconfig.json`
- 修改: `packages/demo-agent/e2b.Dockerfile`
- 删除: `packages/demo-agent/app.py`, `packages/demo-agent/gateway_client.py`, `packages/demo-agent/file_sync.py`, `packages/demo-agent/requirements.txt`

- [ ] **Step 1: 创建目录结构**

```bash
mkdir -p packages/demo-agent/src
```

- [ ] **Step 2: 创建 `packages/demo-agent/src/agent.ts`**

```typescript
import { createAgent } from '@aaas/agent-sdk'

createAgent({
  systemPrompt: 'You are a helpful assistant.',
})
```

- [ ] **Step 3: 创建 `packages/demo-agent/package.json`**

```json
{
  "name": "@aaas/demo-agent",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/agent.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/agent.ts",
    "start": "node dist/agent.js"
  },
  "dependencies": {
    "@aaas/agent-sdk": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.7.0",
    "@types/node": "^24.0.0"
  }
}
```

- [ ] **Step 4: 创建 `packages/demo-agent/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src"]
}
```

- [ ] **Step 5: 修改 `packages/demo-agent/e2b.Dockerfile`**

```dockerfile
FROM node:20-slim
WORKDIR /app
COPY package.json .
RUN npm install --production
COPY dist/ ./dist/
RUN mkdir -p /persistent/shared /persistent/conversation && \
    chmod -R 777 /persistent
CMD ["node", "dist/agent.js"]
```

- [ ] **Step 6: 删除 Python 文件**

```bash
rm packages/demo-agent/app.py
rm packages/demo-agent/gateway_client.py
rm packages/demo-agent/file_sync.py
rm packages/demo-agent/requirements.txt
```

- [ ] **Step 7: 安装依赖并构建**

```bash
pnpm install
pnpm --filter @aaas/demo-agent build
# 期望: dist/agent.js 生成
```

- [ ] **Step 8: Commit**

```bash
git add packages/demo-agent/
git commit -m "feat(demo-agent): rewrite in TypeScript using @aaas/agent-sdk"
```

---

## Task 10：简化 dispatcher `sandbox.ts`

**Files:**
- 修改: `packages/dispatcher/src/sandbox.ts`

移除 `file_sync.py` 和 `app.py` 的启动编排，改为直接启动 Node.js agent。SDK 内部会处理 file sync init、watch 和 HTTP server。

- [ ] **Step 1: 阅读现有 sandbox.ts**

```bash
cat packages/dispatcher/src/sandbox.ts
```

确认以下三段需要移除/替换：
1. `file_sync.py init` 的 try/catch 块（约 20 行）
2. `file_sync.py watch &` 的启动（约 5 行）
3. `python /app/app.py` 的启动（约 5 行）

- [ ] **Step 2: 写失败测试（更新现有测试期望）**

检查是否有 sandbox.ts 的测试：
```bash
find packages/dispatcher/test -name "*.test.ts" | xargs grep -l "sandbox\|flask\|file_sync" 2>/dev/null
```

如有，更新相关测试期望。

- [ ] **Step 3: 修改 `packages/dispatcher/src/sandbox.ts`**

将三段 Python 编排代码替换为：

```typescript
// Start Node.js agent (SDK handles file sync init, watch, and HTTP server internally)
const agentStart = Date.now()
await sandbox.commands.run(
  `nohup bash -c '${envPrefix} node /app/dist/agent.js' > /tmp/agent.log 2>&1 &`,
  { timeoutMs: 10000 }
)
logger.info({ event: 'sandbox.agent_started', conversation_id: conversationId, sandbox_id: sandbox.sandboxId, duration_ms: Date.now() - agentStart })
```

注意保留 `envPrefix` 定义和 `pollHealth` / health check 逻辑不变。如果 SDK 的 file sync init 失败会导致进程退出（agent 不会启动），health check 会超时触发沙箱销毁，与现有行为一致。

- [ ] **Step 4: 运行 dispatcher 测试**

```bash
pnpm --filter @aaas/dispatcher test
# 期望: 通过（或根据 mock 调整）
```

- [ ] **Step 5: Commit**

```bash
git add packages/dispatcher/src/sandbox.ts
git commit -m "feat(dispatcher): simplify sandbox startup — remove python orchestration, SDK handles file sync"
```

---

## Task 11：端到端验证

本地验证 SDK 可以启动并响应 `/chat`。

- [ ] **Step 1: 构建全部包**

```bash
pnpm --filter @aaas/agent-sdk build
pnpm --filter @aaas/demo-agent build
```

- [ ] **Step 2: 在本地模式启动 demo-agent**

需要至少一个 LLM API key：

```bash
cd packages/demo-agent
ANTHROPIC_API_KEY=sk-ant-... node dist/agent.js
# 期望输出:
# [agent-sdk] Local mode: skipping file sync.
# [agent-sdk] Agent listening on port 8080 (local mode)
```

- [ ] **Step 3: 测试 /health**

新开终端：

```bash
curl http://localhost:8080/health
# 期望: {"ok":true}
```

- [ ] **Step 4: 测试 /chat**

```bash
curl -X POST http://localhost:8080/chat \
  -H 'Content-Type: application/json' \
  -d '{"message":"hello, who are you?"}'
# 期望: {"reply":"..."}，reply 不为空
```

- [ ] **Step 5: 停止 demo-agent，运行全部测试**

```bash
pnpm test
# 期望: 全部通过
```

- [ ] **Step 6: 最终 commit**

```bash
git add -A
git commit -m "feat: agent-sdk e2e validated in local mode"
```

---

## 完成标准

- [ ] `pnpm --filter @aaas/agent-sdk test` 全部通过
- [ ] `pnpm --filter @aaas/demo-agent build` 成功
- [ ] 本地启动 demo-agent：`GET /health` 返回 `{ok:true}`，`POST /chat` 返回非空 reply
- [ ] `npx @aaas/agent-sdk init my-agent` 生成可用项目结构
- [ ] dispatcher `sandbox.ts` 不再包含 `file_sync.py` 或 `app.py` 启动逻辑
- [ ] `pnpm test`（全 monorepo）通过
