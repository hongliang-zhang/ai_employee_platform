# {{name}}

An AI agent built with `@alexlikevibe/agent-sdk`.

## Development

```bash
npm install
ANTHROPIC_API_KEY=sk-ant-... npm run dev
curl -X POST http://localhost:8080/chat -H 'Content-Type: application/json' -d '{"message":"hello"}'
```

## Deploy to Agent Runtime

```bash
npm run build
e2b template build
# Register template ID in e2b.toml, then register with the Agent Runtime platform
```
