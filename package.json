{
  "name": "z-mono",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm --filter @ai-employee/platform build",
    "start": "pnpm --filter @ai-employee/platform exec next start",
    "test": "pnpm -r test",
    "test:integration": "pnpm -r --if-present test:integration",
    "lint": "eslint packages"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "tsx": "^4.19.0",
    "typescript-eslint": "^8.0.0"
  }
}
